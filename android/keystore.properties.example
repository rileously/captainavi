# Copy to keystore.properties (gitignored) and fill in local values.
# Never commit keystore.properties or *.keystore / *.jks files.
storePassword=REPLACE_ME
keyPassword=REPLACE_ME
keyAlias=captainavi
storeFile=captainavi-release.keystore
